# Original ROM files
originalGame.3ds
*.cci
*.3ds

# Large extracted ROM filesystem (too big for GitHub)
romfs/
# Keep only the essential modified files
!modified/

# Large reference files
extra_card_list.txt

# Large ROM filesystem directories (too big for GitHub)
romfs/
modified/

# Keep the .gitignore additions from before

# Large ROM filesystem directories (too big for GitHub)
romfs/
modified/

# Keep the .gitignore additions from before
